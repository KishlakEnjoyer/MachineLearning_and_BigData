## Краткая выжимка статьи Захаренко Д.В. «Использование нейронных сетей глубокого обучения для классификации токсичных комментариев»

### 🔍 Цель исследования
Разработка и сравнение эффективности трёх архитектур нейросетей для автоматической классификации токсичных комментариев в социальных сетях с целью снижения нагрузки на модераторов и повышения цифровой цивилизованности.

### 📊 Проблема
- По данным Microsoft (2019), уровень цифровой вежливости достиг четырёхлетнего минимума.
- Токсичные комментарии включают: оскорбления, угрозы, ненависть, непристойности, агрессию.
- Ручная модерация требует огромных временных затрат.

### 🧪 Данные и предобработка
- **Источник**: датасет комментариев Википедии (платформа Kaggle).
- **Классы**: токсичные / нетоксичные (мультиклассовая разметка: сильно токсичные, угрозы, оскорбления и др.).
- **Этапы предобработки**:
  - Приведение к нижнему регистру, раскрытие сокращений.
  - Удаление URL, спецсимволов, не-ASCII символов.
  - Токенизация и индексация через `Tokenizer` (Keras).
  - Лемматизация с использованием `WordNetLemmatizer` (NLTK).
  - Удаление стоп-слов (spaCy + кастомный список однобуквенных/двухбуквенных слов).
  - Заполнение последовательностей до 200 токенов (`pad_sequences`).

### ⚙️ Архитектуры моделей
| Модель | Слои | Обучаемые параметры | Особенности |
|--------|------|---------------------|-------------|
| **LSTM** | 8 слоёв: Embedding → LSTM(50) → Dropout ×2 → Dense ×2 → Pooling | 72 486 | Учитывает последовательную природу текста, запоминает контекст |
| **CNN** | 8 слоёв: Embedding → Conv1D → Dropout → Pooling → Flatten → Dense ×2 | 668 166 | Выявляет n-граммы и локальные паттерны в тексте |
| **LSTM-CNN** | 12 слоёв: комбинация LSTM + Conv1D + BatchNorm + Dropout ×2 + Dense ×3 | 84 008 | Гибридная архитектура, объединяющая преимущества обеих моделей |

### 📈 Результаты (точность)
| Модель | Обучение | Валидация | Публичный тест | Приватный тест |
|--------|----------|-----------|----------------|----------------|
| **LSTM** | 99.22% | 99.26% | **97.33%** | **97.40%** |
| **CNN** | 96.64% | 98.91% | 95.13% | 95.27% |
| **LSTM-CNN** | 98.98% | 99.32% | **97.85%** | **97.65%** |

✅ **Гибридная модель LSTM-CNN показала наилучшую точность** на тестовых данных.

### 💡 Ключевые выводы
1. Глубокое обучение эффективно решает задачу классификации токсичности (точность >95%).
2. Гибридные архитектуры (LSTM-CNN) превосходят «чистые» модели за счёт комбинирования контекстного анализа и выявления локальных паттернов.
3. Критически важна качественная предобработка текста (особенно подбор стоп-слов).
4. Для дальнейшего улучшения рекомендуется:
   - Применение методов подбора гиперпараметров (GridSearchCV).
   - Использование более репрезентативных датасетов.
   - Учёт языковых нюансов (сарказм, ирония, культурные особенности).

### 🌐 Практическая значимость
Автоматизированная классификация токсичных комментариев позволяет платформам:
- Сократить время модерации в десятки раз.
- Обеспечить гибкость в политике обработки контента.
- Создать более безопасную и уважительную среду общения.